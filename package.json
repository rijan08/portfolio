{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Rijan S — personal portfolio",
  "scripts": {
    "build": "node build.mjs",
    "build:css": "tailwindcss -i ./css/input.css -o ./css/styles.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
